# Server-only Neon database URLs.
# Use a separate value per Vercel environment.
DATABASE_URL=
DATABASE_URL_PREVIEW=
DATABASE_URL_PRODUCTION=

# Optional: map/AI providers stay server-only as well.
MAP_PROVIDER_KEY=
AI_API_KEY=
